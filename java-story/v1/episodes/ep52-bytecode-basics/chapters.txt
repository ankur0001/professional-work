0:00 Episode Fifty-One showed how the JVM loads class files.
0:03 But what is actually inside those bytes?
0:06 Java source compiles to bytecode — a stack-machine…
0:10 Opcodes like iload, invokevirtual, and return drive every…
0:15 javap disassembles class files so you can read what the…
0:19 Today — bytecode basics, the constant pool, and the stack…
0:24 Episode Fifty-Two.
0:26 Bytecode Basics.
0:28 A class file is a structured binary format — not…
0:32 Magic number CA FE BA BE identifies a valid Java class file.
0:36 Constant pool holds strings, class names, method…
0:41 Fields, methods, and attributes describe the class…
0:45 Code attribute contains the actual bytecode instructions…
0:50 The JVM never sees your .java file — only verified .class…
0:55 javap is the JDK disassembler — your window into bytecode.
0:59 javap -c MyClass prints disassembled method bodies.
1:02 javap -v adds verbose output — constant pool entries and…
1:07 javap -p shows private members — useful for debugging…
1:12 Compare source to javap output — see what the compiler…
1:17 Every senior Java developer should read javap at least…
1:22 The JVM is a stack machine — operands live on an operand…
1:26 Each method frame has its own operand stack and local…
1:31 Instructions push values, operate, and pop results.
1:34 iload pushes a local int — iadd pops two ints and pushes…
1:39 No general-purpose registers — the stack is the workspace.
1:43 Think push, operate, pop — that mental model unlocks every…
1:48 Opcodes are single-byte instructions — some have operands.
1:52 Constants — iconst_1, ldc, bipush load values onto the…
1:57 Locals — iload, istore, aload, astore read and write local…
2:02 Fields — getfield, putfield, getstatic access object and…
2:07 Methods — invokevirtual, invokestatic, invokespecial…
2:13 Control flow — ifeq, goto, tableswitch branch on stack…
2:17 Walk through a simple method bytecode by bytecode.
2:21 aload_0 pushes this — getfield reads an instance field.
2:24 invokevirtual calls a method — return ends the frame.
2:28 Stack depth must match what verification expects — stack…
2:32 Compiler optimizations change bytecode — loops may unroll…
2:37 Reading bytecode connects source code to runtime behavior.
2:41 Three common mistakes.
2:42 One — assuming bytecode matches source line-for-line —…
2:47 Two — ignoring stack depth errors — VerifyError at class…
2:52 Three — confusing invokevirtual with invokestatic — wrong…
2:57 Also — editing .class files by hand without understanding…
3:02 Use javap as a learning tool — not as something to fear.
3:06 Interview question — what is Java bytecode?
3:09 Platform-independent instruction set for the JVM stack…
3:13 Compiled from .java by javac into .class files.
3:17 Constant pool, fields, methods, and Code attributes per…
3:21 javap disassembles bytecode — read opcodes like iload and…
3:26 Verification ensures type safety and stack consistency…
3:31 Bytecode runs on stacks — but where do objects actually…
3:35 Episode Fifty-Three — Heap and Stack.
3:38 Frames, locals, object layout, and metaspace.
3:41 See you there.
