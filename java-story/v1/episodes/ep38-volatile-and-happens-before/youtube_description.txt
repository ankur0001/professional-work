The Java Story — Episode 38: volatile and Happens-Before
Original cut.

The Java Story is a structured series that teaches Java from first principles through modern production practice — one clear episode at a time.

In this episode:
• Synchronization prevents races
• Without visibility guarantees, a thread may read a stale cached…
• The Java Memory Model defines when writes become visible across…
• volatile and happens-before are the vocabulary for that contract.
• Locks help — but visibility has its own rules.
• Today — volatile, happens-before, and memory visibility in Java.
• …and more

Timestamps
0:00 Synchronization prevents races
0:04 Without visibility guarantees, a thread may read a stale…
0:09 The Java Memory Model defines when writes become visible…
0:14 volatile and happens-before are the vocabulary for that…
0:18 Locks help — but visibility has its own rules.
0:21 Today — volatile, happens-before, and memory visibility in…
0:25 Episode Thirty-Eight.
0:26 volatile and Happens-Before — memory visibility.
0:30 Each thread may cache field values in CPU registers or…
0:35 A write by thread A might sit in a cache — invisible to…
0:39 Synchronization flushes caches — but you cannot…
0:43 You need lighter-weight visibility guarantees for flags…
0:47 Reading a stale boolean can keep a worker loop running…
0:51 Visibility bugs look like logic errors — the code path…
0:55 volatile tells the JVM — reads and writes go directly to…
1:00 No caching of the volatile field in thread-local storage.
1:03 A read of a volatile always sees the latest write by any…
1:07 volatile does not make compound operations atomic — count…
1:13 Use volatile for single-field flags — running, ready,…
1:17 volatile is about visibility — not mutual exclusion.
1:20 Happens-before is the formal ordering rule in the JMM.
1:23 If action A happens-before B, then B sees everything A did.
1:27 Unlocking a monitor happens-before the next lock on that…
1:31 Writing a volatile happens-before a subsequent read of…
1:35 Thread start and join establish happens-before edges too.
1:38 Chain these edges to reason about what each thread can…
1:42 The Java Memory Model — the contract between compiler,…
1:47 Without reordering, CPUs could not pipeline — performance…
1:51 The JMM allows optimizations within happens-before…
1:55 Program order within a single thread is preserved — as-if…
1:59 Across threads — only guaranteed ordering comes from…
2:03 Understand the JMM — or your concurrent code will surprise…
2:07 When to use volatile.
2:08 One writer, many readers — status flags and configuration…
2:13 Publishing an immutable object reference — safe if object…
2:18 Double-checked locking requires volatile on the reference…
2:22 When not — counters, compound updates, or multiple writers.
2:25 For those — use synchronized or atomic classes instead.
2:29 Three common mistakes.
2:30 One — using volatile on a non-volatile field inside a…
2:36 Two — assuming volatile makes increment atomic — it does…
2:40 Three — relying on visibility without establishing…
2:44 Also — double-checked locking without volatile — broken on…
2:49 Match the tool to the guarantee you actually need.
2:53 Interview question — what does volatile guarantee?
2:56 Visibility — every read sees the latest write to that field.
3:00 Ordering — writes before a volatile read are visible to…
3:04 Not atomicity — compound operations still need…
3:08 Contrast with synchronized — which provides both exclusion…
3:12 Mention happens-before as the formal backing concept.
3:16 Intrinsic locks work
3:19 Episode Thirty-Nine — Explicit Locks.
3:22 ReentrantLock, tryLock, and Condition variables.
3:25 See you there.

Resources
- Captions: included (SRT in repo)
- Narration / transcript: see narration.md
- Series index: java-story/v1/INDEX.md

If this helped, like the video and subscribe for the next episode.
Comment with questions — I read them.

#Java #JavaTutorial #Programming #TheJavaStory #Episode38
