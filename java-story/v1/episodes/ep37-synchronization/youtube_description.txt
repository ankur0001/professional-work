The Java Story — Episode 37: Synchronization
Original cut.

The Java Story is a structured series that teaches Java from first principles through modern production practice — one clear episode at a time.

In this episode:
• Two threads update the same counter
• Race conditions happen when shared mutable state is accessed without…
• Synchronization is how Java makes critical sections safe.
• One thread at a time — mutual exclusion on shared data.
• Locks, monitors, and the synchronized keyword — the first line of…
• Today — synchronized methods, blocks, and intrinsic locks.
• …and more

Timestamps
0:00 Two threads update the same counter
0:04 Race conditions happen when shared mutable state is…
0:09 Synchronization is how Java makes critical sections safe.
0:13 One thread at a time — mutual exclusion on shared data.
0:16 Locks, monitors, and the synchronized keyword — the first…
0:21 Today — synchronized methods, blocks, and intrinsic locks.
0:24 Episode Thirty-Seven.
0:26 Synchronization — safe access to shared data.
0:30 A race condition — outcome depends on thread scheduling…
0:34 count plus plus is not atomic — read, increment, write —…
0:38 Two threads interleave those steps — updates can be lost.
0:42 The bug is intermittent — hardest kind to reproduce.
0:45 You need mutual exclusion — only one thread in the…
0:49 Synchronization enforces that rule at the language level.
0:53 synchronized on a method locks the instance — or the Class…
0:57 Only one thread can execute that synchronized method at a…
1:01 Other threads block until the lock is released.
1:04 Simple and readable for small critical sections.
1:07 The lock is automatically released when the method exits —…
1:11 Use synchronized methods when the whole method is the…
1:15 synchronized block — finer control over what is protected.
1:19 synchronized on this — locks the current instance.
1:22 synchronized on a dedicated lock object — often clearer…
1:26 Protect only the few lines that touch shared state.
1:29 Smaller critical sections mean less contention — better…
1:33 Prefer blocks when only part of a method needs protection.
1:37 Every Java object has an intrinsic lock — also called a…
1:41 Entering synchronized acquires the monitor
1:45 Reentrant — the same thread can acquire a lock it already…
1:49 wait and notify operate on the monitor — coordination…
1:53 The JVM maps monitors to operating-system mutexes under…
1:57 Understand monitors — they underpin every synchronized…
2:02 When to synchronize.
2:03 Any read-modify-write on shared mutable fields.
2:06 Invariants that must hold while multiple fields are updated.
2:10 Compound actions — check-then-act on shared state.
2:13 When not — over-synchronizing everything kills performance.
2:17 Synchronize the minimum — but synchronize what matters.
2:20 Three common mistakes.
2:22 One — synchronizing on the wrong object — String literals…
2:27 Two — holding locks while doing slow I/O — blocks every…
2:31 Three — nested locks on different objects — classic…
2:35 Also — assuming synchronized fixes visibility alone — see…
2:39 Lock only what you must — for as short as possible.
2:43 Interview question — synchronized method versus…
2:47 Both acquire an intrinsic lock on an object.
2:50 Method form locks on this or the Class
2:54 Blocks allow finer granularity — protect fewer lines.
2:58 Mention reentrancy and that locks release on exception.
3:01 That answer shows you understand monitors, not just…
3:05 Locks prevent races
3:09 Episode Thirty-Eight — Memory Visibility.
3:11 volatile, happens-before, and the Java Memory Model.
3:15 See you there.

Resources
- Captions: included (SRT in repo)
- Narration / transcript: see narration.md
- Series index: java-story/v1/INDEX.md

If this helped, like the video and subscribe for the next episode.
Comment with questions — I read them.

#Java #JavaTutorial #Programming #TheJavaStory #Episode37
