The Java Story — Episode 51: Class Loading
Original cut.

The Java Story is a structured series that teaches Java from first principles through modern production practice — one clear episode at a time.

In this episode:
• Episode Fifty showed virtual threads running your code.
• But how does the JVM find and load that code in the first place?
• Every class file travels through loading, linking, and initialization.
• ClassLoader hierarchy — bootstrap, platform, application — delegates…
• Linkage verifies bytecode and prepares static fields.
• Today — class loading basics, the loader tree, and initialization…
• …and more

Timestamps
0:00 Episode Fifty showed virtual threads running your code.
0:03 But how does the JVM find and load that code in the first…
0:07 Every class file travels through loading, linking, and…
0:11 ClassLoader hierarchy — bootstrap, platform, application —…
0:16 Linkage verifies bytecode and prepares static fields.
0:19 Today — class loading basics, the loader tree, and…
0:23 Episode Fifty-One.
0:25 Class Loading Basics.
0:27 Three built-in loaders in the JDK class-loading hierarchy.
0:31 Bootstrap ClassLoader — loads core JDK classes from…
0:36 Written in native code — returns null from getClassLoader…
0:41 Platform ClassLoader — JDK modules not on the boot path.
0:45 Application ClassLoader — loads classes from the classpath…
0:50 Default context loader for app code — Thread.currentThread…
0:55 Loading — find the bytecode and define the Class object.
0:59 findClass reads bytes — defineClass creates the runtime…
1:03 Parent delegation — child asks parent first before loading…
1:07 Prevents duplicate class definitions across loaders.
1:10 Custom ClassLoader — override findClass for hot reload or…
1:14 Class.forName triggers loading — does not initialize by…
1:18 Linking has three sub-phases — verification, preparation,…
1:23 Verification — bytecode safety checks — stack map tables,…
1:28 Preparation — allocate static field memory, set primitives…
1:33 Resolution — replace symbolic references with direct…
1:39 Linking happens after loading, before initialization.
1:42 Failed verification throws VerifyError — class never runs.
1:46 Initialization — run the static initializer and assign…
1:51 Triggered on first active use — new, static field access,…
1:56 Class.forName with initialize true runs clinit.
2:00 JVM guarantees clinit runs exactly once per class per…
2:04 Parent classes initialize before children.
2:07 Deadlocks possible if static blocks acquire locks in…
2:11 The delegation model in practice.
2:13 Application loader asks platform — platform asks bootstrap.
2:17 Only if parent cannot find the class does the child…
2:22 SPI pattern breaks delegation — Thread context class loader.
2:26 ServiceLoader uses context loader to find provider…
2:30 Understand which loader owns a class — affects visibility…
2:35 Three common mistakes.
2:36 One — Class.forName in static blocks causing…
2:40 Two — assuming custom loaders without parent delegation —…
2:45 Three — mixing classes from different loaders —…
2:50 Also — leaking ClassLoader references — PermGen or…
2:55 Class loading is infrastructure — get the loader…
2:59 Interview question — explain class loading phases?
3:02 Loading — find bytes, define Class
3:08 Initialization — run static initializer on first active use.
3:12 Bootstrap, platform, application — parent delegation model.
3:16 Custom ClassLoader for plugins — override findClass,…
3:21 Mention Class.forName versus ClassLoader loadClass…
3:26 Classes load from bytecode — but what is inside those…
3:31 Episode Fifty-Two — Bytecode Basics.
3:34 Opcodes, constant pool, and reading javap output.
3:37 See you there.

Resources
- Captions: included (SRT in repo)
- Narration / transcript: see narration.md
- Series index: java-story/v1/INDEX.md

If this helped, like the video and subscribe for the next episode.
Comment with questions — I read them.

#Java #JavaTutorial #Programming #TheJavaStory #Episode51
