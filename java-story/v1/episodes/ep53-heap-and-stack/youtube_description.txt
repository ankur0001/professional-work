The Java Story — Episode 53: Heap and Stack
Original cut.

The Java Story is a structured series that teaches Java from first principles through modern production practice — one clear episode at a time.

In this episode:
• Episode Fifty-Two decoded bytecode on the operand stack.
• But where do method frames and objects actually live in memory?
• Each thread owns a stack of frames — locals and operand stacks inside.
• The heap holds every object your program allocates with new.
• Class metadata lives in metaspace — separate from the object heap.
• Today — heap and stack, frames, locals, and object layout.
• …and more

Timestamps
0:00 Episode Fifty-Two decoded bytecode on the operand stack.
0:03 But where do method frames and objects actually live in…
0:07 Each thread owns a stack of frames — locals and operand…
0:11 The heap holds every object your program allocates with new.
0:15 Class metadata lives in metaspace — separate from the…
0:19 Today — heap and stack, frames, locals, and object layout.
0:23 Episode Fifty-Three.
0:25 Heap and Stack.
0:26 Each Java thread has its own call stack — one frame per…
0:30 A frame stores local variables, the operand stack, and a…
0:36 When a method is invoked, a new frame is pushed — return…
0:40 StackOverflowError means too many nested calls — usually…
0:45 Frames are thread-local — no sharing between threads on…
0:49 The stack is fast and automatically reclaimed when a…
0:53 Local variable slot zero is always this for instance…
0:57 Parameters occupy the next slots — iload and istore…
1:01 Wide types like long and double consume two consecutive…
1:05 The operand stack is separate from locals — temporary…
1:09 Compiler assigns slot numbers — visible in javap with -v.
1:13 Locals die with the frame — no manual cleanup needed.
1:16 Every new keyword allocates an object on the heap.
1:19 All threads share the heap — objects are visible across…
1:24 References on the stack or in other objects point to heap…
1:28 Heap memory is managed by the garbage collector — not…
1:33 OutOfMemoryError means the heap cannot grow further.
1:36 Large object graphs live here — caches, collections,…
1:41 A heap object starts with a mark word and a klass pointer…
1:45 Instance fields follow the header — primitives inline,…
1:49 Arrays store length then elements — int arrays pack ints…
1:54 Object size depends on header plus fields plus alignment…
1:58 Compressed oops shorten reference fields on 64-bit JVMs…
2:03 Understanding layout helps reason about memory footprint…
2:07 Class metadata — method tables, constant pools, field…
2:13 Metaspace replaced PermGen in Java 8 — native memory, not…
2:18 Grows as classes load — reclaimed when ClassLoader is…
2:22 ClassLoader leaks can exhaust metaspace — a common…
2:26 Flag MaxMetaspaceSize caps growth — default is effectively…
2:30 Heap holds objects — metaspace holds class definitions.
2:34 Three common mistakes.
2:35 One — storing large objects on the stack — only references…
2:40 Two — assuming stack variables are thread-safe — only if…
2:45 Three — ignoring metaspace when leaking ClassLoaders in…
2:50 Also — confusing heap size with total JVM memory footprint.
2:54 Know which memory region each piece of data occupies.
2:57 Interview question — heap versus stack in Java?
3:01 Stack — per-thread frames with locals and operand stacks —…
3:06 Heap — shared object storage — garbage collected.
3:09 References on stack point to objects on heap.
3:12 Metaspace holds class metadata — separate from heap since…
3:16 StackOverflowError versus OutOfMemoryError — different…
3:21 Objects on the heap outlive their frames — something must…
3:26 Episode Fifty-Four — Garbage Collection Intro.
3:30 Roots, mark-sweep, generations, and stop-the-world pauses.
3:34 See you there.

Resources
- Captions: included (SRT in repo)
- Narration / transcript: see narration.md
- Series index: java-story/v1/INDEX.md

If this helped, like the video and subscribe for the next episode.
Comment with questions — I read them.

#Java #JavaTutorial #Programming #TheJavaStory #Episode53
