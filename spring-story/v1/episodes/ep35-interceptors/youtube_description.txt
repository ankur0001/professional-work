The Spring Story — Episode 35: Interceptors

The Spring Story is a structured series that teaches Spring Framework and Spring Boot from first principles through production practice — one clear episode at a time.

In this episode:
• Interceptors speak Spring MVC: handlers, handler mappings, and the lifecycle around controller invocation.
• The port operations console makes that distinction concrete — you want timing on /admin/** handler…
• Recall the dispatcher pipeline.
• After HandlerMapping resolves a HandlerExecutionChain, that chain includes interceptors.
• DispatcherServlet calls preHandle on each interceptor before the HandlerAdapter invokes the controller.
• After a successful invocation it calls postHandle.
• After completion — including when an exception was thrown — it calls afterCompletion.
• That timeline is inside MVC, after a handler was chosen.
• …and more

Timestamps
0:00 Filters speak Servlet. Interceptors speak Spring MVC: handlers,…
0:35 Path patterns on the registry are the point of this lesson’s…
1:16 postHandle runs only after a successful handler return and before…
1:55 Servlet filters cover early HTTP wrapping; MVC interceptors cover…

Resources
- Narration / transcript: see narration.md in this episode folder
- Series index: spring-story/v1/INDEX.md

Next up: Episode 36 — File Upload

If this helped, like the video and subscribe for the next episode.
Comment with questions — I read them.

#Spring #SpringBoot #Java #TheSpringStory
