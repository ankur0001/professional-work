The Spring Story — Episode 110: Event-Driven Architecture

The Spring Story is a structured series that teaches Spring Framework and Spring Boot from first principles through production practice — one clear episode at a time.

In this episode:
• After Berth.reserve succeeds, scheduling used to call billing over Feign, then the yard board, then…
• If billing was down, did the reservation roll back?
• Event-driven architecture decouples that timeline: scheduling publishes BerthReserved; billing and others…
• Distinguish in-process events from broker events.
• Spring’s ApplicationEventPublisher notifies listeners inside the same JVM — useful for clearing caches or…
• Cross-service decoupling needs a broker (Kafka via Cloud Stream, for example) and delivery rules you can…
• Mixing them without care — a transactional listener that assumes local and remote are the same — is how…
• AFTER_COMMIT matters: listeners that run before commit can publish a reservation that rolls back.
• …and more

Timestamps
0:00 Synchronous fan-out hurts. After Berth.reserve succeeds,…
0:42 AFTERCOMMIT matters: listeners that run before commit can publish…
1:18 Consumers must be idempotent; at-least-once delivery will replay…
2:12 Trade-offs: decoupling availability and fan-out versus harder…
2:58 A misconception is replacing every API with events until a trucker…

Resources
- Narration / transcript: see narration.md in this episode folder
- Series index: spring-story/v1/INDEX.md

Next up: Episode 111 — CQRS

If this helped, like the video and subscribe for the next episode.
Comment with questions — I read them.

#Spring #SpringBoot #Java #TheSpringStory
