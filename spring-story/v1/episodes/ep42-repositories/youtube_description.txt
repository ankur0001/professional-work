The Spring Story — Episode 42: Repositories

The Spring Story is a structured series that teaches Spring Framework and Spring Boot from first principles through production practice — one clear episode at a time.

In this episode:
• You understand entities, lifecycle, and the persistence context.
• You still do not want every berth planner use case to inject EntityManager and write the same…
• Spring Data JPA repositories are the typed façade — interfaces that become working beans at startup.
• Declare what you need for vessels.
• Spring Data supplies the implementation.
• JpaRepository already gives you save, saveAll, findById, findAll, deleteById, pagination, and flush helpers.
• Derived query methods parse the method name: findByImoNumber becomes a query on the imoNumber property —…
• You stay in the entity model — property names, not column names — until you drop to @Query or native SQL…
• …and more

Timestamps
0:00 You understand entities, lifecycle, and the persistence context.…
0:47 How does the proxy work? At startup, Spring Data scans for…
1:25 Custom implementations exist when derived names and @Query are not…
1:56 That bulk update returns the number of rows touched. It is…

Resources
- Narration / transcript: see narration.md in this episode folder
- Series index: spring-story/v1/INDEX.md

Next up: Episode 43 — Relationships

If this helped, like the video and subscribe for the next episode.
Comment with questions — I read them.

#Spring #SpringBoot #Java #TheSpringStory
