The Spring Story — Episode 78: Flux

The Spring Story is a structured series that teaches Spring Framework and Spring Boot from first principles through production practice — one clear episode at a time.

In this episode:
• Mono was zero or one vessel.
• Harbor ops still need live maps: AIS position ticks for every ship on a quay, berth event feeds,…
• That cardinality is Flux<T> — a reactive sequence of zero to many items.
• The mental model stays a publisher: assemble operators into a pipeline, subscribe (or return the publisher…
• Nothing in the chain runs until something subscribes.
• Walk an operator opening the quay map.
• WebFlux subscribes to the returned Flux when the SSE response starts.
• aisFeed.positionsForQuay(quayId) begins emitting AisFix items — each fix is one signal, not a buffered…
• …and more

Timestamps
0:00 Mono was zero or one vessel. Harbor ops still need live maps: AIS…
1:32 Operator vocabulary that differs from Mono mostly by cardinality.…
2:17 Hot versus cold shows up harder with Flux. A cold publisher — a…
3:00 Failure mode symptoms on the ops floor. Unbounded flatMap without…
3:51 Trade-offs. Flux expresses push sequences and composes…
4:39 We can now express sequences. CPU work, blocking legacy PDF calls,…

Resources
- Narration / transcript: see narration.md in this episode folder
- Series index: spring-story/v1/INDEX.md

Next up: Episode 79 — Schedulers

If this helped, like the video and subscribe for the next episode.
Comment with questions — I read them.

#Spring #SpringBoot #Java #TheSpringStory
