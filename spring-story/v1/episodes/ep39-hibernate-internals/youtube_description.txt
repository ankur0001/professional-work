The Spring Story — Episode 39: Hibernate Internals

The Spring Story is a structured series that teaches Spring Framework and Spring Boot from first principles through production practice — one clear episode at a time.

In this episode:
• A planner moves vessel IMO-9312345 from berth B2 to berth B7.
• Your service loads a BerthAssignment, calls reassign("B7"), and returns.
• Yet the database row changes.
• That quiet write is Hibernate dirty checking — and it is the internal you need before lifecycle jargon…
• When Hibernate loads an entity inside a persistence context, it keeps a snapshot of the loaded state.
• On flush — at transaction commit by default, or earlier if you force it — Hibernate compares the current…
• Unchanged entities produce no update.
• That is the unit-of-work idea: you mutate objects; the provider schedules SQL.
• …and more

Timestamps
0:00 A planner moves vessel IMO-9312345 from berth B2 to berth B7. Your…
0:42 On flush, Hibernate may emit:
1:25 The first-level cache is the same persistence context: load…
2:11 Dirty checking explains how managed entities write themselves. It…

Resources
- Narration / transcript: see narration.md in this episode folder
- Series index: spring-story/v1/INDEX.md

Next up: Episode 40 — Entity Lifecycle

If this helped, like the video and subscribe for the next episode.
Comment with questions — I read them.

#Spring #SpringBoot #Java #TheSpringStory
